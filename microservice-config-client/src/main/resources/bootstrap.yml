spring:
  application:
    name: microservice-foo
  cloud:
    config:
      uri: http://localhost:8090
      profile: dev
      label: master
  kafka:
      bootstrap-servers: localhost:9092
      client-id: asd2
      consumer:
        enable-auto-commit: true
        group-id: asdasd2